plan:
 - default
profiles:
  default:
    script: "/home/ubuntu/foo.sh"
#    tests:
#      - "bash -c 'pwd && ls -la'"
    environment:
      'FOOBARVAR': 'foobarval'
      'PATH2': '/home/ubuntu/tmp'
    timeout: 300
